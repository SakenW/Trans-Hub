# ==============================================================================
#  Trans-Hub 示例配置文件 (.env.example)
# ==============================================================================
#
# 使用说明：
# 1. 将此文件复制或重命名为 .env
#    (在命令行中: cp .env.example .env)
# 2. 打开 .env 文件，修改下面的值，填入你自己的配置。
# 3. .env 文件包含敏感信息（如 API 密钥），请确保它已被添加到 .gitignore 文件中，
#    绝不要将 .env 文件提交到你的版本控制系统（如 Git）。
#
# ==============================================================================


# ------------------------------------------------------------------------------
#  OpenAI 引擎配置
# ------------------------------------------------------------------------------
# 要启用 OpenAI 引擎，请取消注释并提供以下配置。

# [必需] 你的 OpenAI 兼容 API 的地址。
# 我们使用 ENDPOINT 而不是 BASE_URL 是为了避免我们开发中遇到的解析问题。
TH_OPENAI_ENDPOINT="https://api.openai.com/v1"

# [必需] 你的 API 密钥。
# 即使你的服务不需要密钥，也建议填入一个非空字符串，例如 "not-needed"。
TH_OPENAI_API_KEY="sk-your-secret-key-here"

# [可选] 你希望使用的模型名称。
# 如果不提供，将使用代码中定义的默认值 (例如 "gpt-4o")。
TH_OPENAI_MODEL="gpt-3.5-turbo"


# ------------------------------------------------------------------------------
#  DeepL 引擎配置 (未来扩展示例)
# ------------------------------------------------------------------------------
# 当我们未来添加 DeepL 引擎后，你可以在这里配置它。
#
# [必需] 你的 DeepL API 密钥。
# TH_DEEPL_API_KEY="your-deepl-api-key"
#
# [可选] DeepL API 的类型 ("free" 或 "pro")。
# TH_DEEPL_SERVER_TYPE="free"


# ------------------------------------------------------------------------------
#  其他全局配置 (未来扩展示例)
# ------------------------------------------------------------------------------
# 你可以在这里覆盖代码中定义的默认日志级别等。
#
# TRANS_HUB_LOG_LEVEL="DEBUG"