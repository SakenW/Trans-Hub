# ==============================================================================
#  Trans-Hub 示例配置文件 (.env)
# ==============================================================================
#
# 使用说明：
# 1. 将此文件复制或重命名为 .env
# 2. 修改下面的值以匹配您的环境。
# 3. pydantic-settings 会自动加载此文件中的环境变量。
#
# ==============================================================================

# ------------------------------------------------------------------------------
#  核心配置
# ------------------------------------------------------------------------------
# 数据库连接 URL。对于生产环境，推荐使用绝对路径。
# TH_DATABASE_URL="sqlite:////var/data/transhub.db"

# 默认的源语言代码。
# TH_SOURCE_LANG="en"

# 默认激活的翻译引擎。
# TH_ACTIVE_ENGINE="translators"

# ------------------------------------------------------------------------------
#  监控配置 (Metrics)
# ------------------------------------------------------------------------------
# 是否启用 Prometheus 指标。默认值为 "false"。
# 要启用，请设置为 "true"。
# TH_METRICS_ENABLED="true"

# 如果启用，Prometheus 指标将在哪个端口上暴露（用于 FastAPI 服务）。
# TH_METRICS_PORT="8001"


# ------------------------------------------------------------------------------
#  OpenAI 引擎配置
# ------------------------------------------------------------------------------
# 要使用 "openai" 引擎，请取消以下变量的注释并填入您的凭据。

# [必需] 你的 OpenAI 兼容 API 的地址。
# TH_OPENAI_ENDPOINT="https://api.openai.com/v1"

# [必需] 你的 API 密钥。
# TH_OPENAI_API_KEY="sk-your-secret-key-here"

# [可选] 你希望使用的模型名称，默认为 "gpt-3.5-turbo"。
# TH_OPENAI_MODEL="gpt-4o"