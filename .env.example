# ==============================================================================
#  Trans-Hub 示例配置文件 (.env.example)
# ==============================================================================
#
# 使用说明：
# 1. 将此文件复制或重命名为 .env
#    (在命令行中: cp .env.example .env)
# 2. 打开 .env 文件，修改下面的值，填入你自己的配置。
# 3. .env 文件包含敏感信息，请确保它已被添加到 .gitignore 文件中。
#
# ==============================================================================


# ------------------------------------------------------------------------------
#  OpenAI 引擎配置 (需要时取消注释)
# ------------------------------------------------------------------------------
# 要在您的应用中激活 OpenAI 引擎 (config = TransHubConfig(active_engine="openai")),
# 请取消以下变量的注释并填入您的凭据。

# [必需] 你的 OpenAI 兼容 API 的地址。
TH_OPENAI_ENDPOINT="https://api.openai.com/v1"

# [必需] 你的 API 密钥。
TH_OPENAI_API_KEY="sk-your-secret-key-here"

# [可选] 你希望使用的模型名称。
# 如果不提供，将使用代码中定义的默认值 "gpt-3.5-turbo"。
TH_OPENAI_MODEL="gpt-4o"


# ------------------------------------------------------------------------------
#  Translators 引擎配置 (可选的微调)
# ------------------------------------------------------------------------------
# 'translators' 引擎是默认引擎，无需任何配置即可工作。
# 但如果您想覆盖默认的服务商 (provider)，可以取消下面的注释。

# [可选] 指定 'translators' 引擎使用的服务商。
# 默认值为 "google"。其他可用选项包括 "bing", "deepl" 等。
# TH_TRANSLATORS_PROVIDER="bing"


# ------------------------------------------------------------------------------
#  DeepL 引擎配置 (未来扩展示例)
# ------------------------------------------------------------------------------
# 当我们未来添加 DeepL 引擎后，你可以在这里配置它。
#
# TH_DEEPL_API_KEY="your-deepl-api-key"